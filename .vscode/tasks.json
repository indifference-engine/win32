{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Make and run example",
      "group": {
        "isDefault": true,
        "kind": "build"
      },
      "type": "shell",
      "command": "clear && (make -j || (clear && make)) && ./dist/example.exe",
    }
  ]
}
